apply plugin: 'java'

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
    compile 'com.google.code.gson:gson:2.8.1'
    compile project(':parserlibrary')
}

sourceCompatibility = "1.7"
targetCompatibility = "1.7"
